import {Video} from "../components/Video";

export const Explore = () => {
  return (
    <main>
      {/* fetch the videos from backend and map them here below by passing the props */}
      <Video />
    </main>
  );
};
